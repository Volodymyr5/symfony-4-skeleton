{% extends 'base.html.twig' %}

{% block body %}

    <a href="{{ path('connect_google') }}">Sign in with Google</a>
    <a href="{{ path('connect_facebook') }}">Sign in with Facebook</a>
    <a href="{{ path('security_login') }}">Login</a>
    <a href="{{ path('security_logout') }}">Logout</a>

{% endblock %}